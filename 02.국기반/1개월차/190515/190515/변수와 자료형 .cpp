#include <iostream>

using namespace std;
/*
void main()
{
	// ##1. 변수와 자료형
	//int PlayerHp = 1000;

	// PlayerHp는 변수이다.
	// 변수는 값을 저장하는 저장공간(메모리)이다.
	// 그리고 그 메모리에는 이름이 부여 된다.
	// 메모리에 저장된 값은 변수를 통해서 얼마든지 접근 및 재사용할 수 있다.
	// 변수에 담긴 값은 얼마든지 수정 가능하다.

	// int는 자료형이다.
	// 자료형은 변수에 어떤 유형의 데이터를 저장할지 결정.
	// 자료형은 변수의 크기 (저장 공간의 크기)를 결정.

	// ##2. 변수 선언.
	// 선언 : 변수를 사용할 준비를 하는 행위.
	//int PlayerMp; // 변수 선언. 현재 메모리 공간에 4바이트 공간이 할당 됨.
	//PlayerMp = 1000; // 변수 초기화. 변수에 최초 값을 저장하는 행위.

	//int PlayerAtt = 500; // 선언과 동시에 초기화.
	// playerAtt이라는 변수가 담고있는 값을 읽어서 화면에 출력.
	//cout << PlayerAtt << endl;

	//H e l l o w o r l d 	

	char H = 72;
	char e = 101;
	char l = 108;
	char o = 111;
	char W = 87;
	char r = 114;
	char d = 100;


	cout << H << e << l << l << o << endl;
}*/